<Window x:Class="Pos.Client.Wpf.Windows.Sales.TillSessionSummaryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Till Session Summary" Height="560" Width="720"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="Outlet:" VerticalAlignment="Center"/>
            <TextBlock x:Name="OutletText"   Margin="6,0,16,0" FontWeight="Bold" VerticalAlignment="Center"
                       Text="{Binding OutletId}" />
            <TextBlock Text="Counter:" VerticalAlignment="Center"/>
            <TextBlock x:Name="CounterText"  Margin="6,0,16,0" FontWeight="Bold" VerticalAlignment="Center"
                       Text="{Binding CounterId}" />
            <TextBlock Text="Till ID:" VerticalAlignment="Center"/>
            <TextBlock x:Name="TillIdText"   Margin="6,0,16,0" FontWeight="Bold" VerticalAlignment="Center"
                       Text="{Binding TillId}" />
            <TextBlock Text="Opened:" VerticalAlignment="Center"/>
            <TextBlock x:Name="OpenedText"   Margin="6,0,16,0" VerticalAlignment="Center"
                       Text="{Binding OpenedText}" />
            <TextBlock Text="Duration:" VerticalAlignment="Center"/>
            <TextBlock x:Name="DurationText" Margin="6,0,0,0" VerticalAlignment="Center"
                       Text="{Binding DurationText}" />
        </StackPanel>

        <!-- Body -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Left pane: Money -->
            <GroupBox Header="Money" Grid.Column="0" Margin="0,0,8,0">
                <Grid Margin="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="8"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="8"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="180" />
                    </Grid.ColumnDefinitions>

                    <!-- Sales totals -->
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Sales total:" HorizontalAlignment="Right"/>
                    <TextBlock Grid.Row="0" Grid.Column="1" x:Name="SalesTotalText" TextAlignment="Right" FontWeight="Bold"
                               Text="{Binding SalesTotal}" />

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Returns total:" HorizontalAlignment="Right"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" x:Name="ReturnsTotalText" TextAlignment="Right" FontWeight="Bold"
                               Text="{Binding ReturnsTotal}" />

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Net total:" HorizontalAlignment="Right"/>
                    <TextBlock Grid.Row="2" Grid.Column="1" x:Name="NetTotalText" TextAlignment="Right" FontWeight="Bold"
                               Text="{Binding NetTotal}" />

                    <Border Grid.Row="3" Grid.ColumnSpan="2" BorderBrush="#EEE" BorderThickness="0,1,0,0" Margin="0,6,0,6"/>

                    <!-- Cash -->
                    <TextBlock Grid.Row="4" Grid.Column="0" Text="Opening float:" HorizontalAlignment="Right"/>
                    <TextBlock Grid.Row="4" Grid.Column="1" x:Name="OpeningFloatText" TextAlignment="Right"
                               Text="{Binding OpeningFloat}" />

                    <TextBlock Grid.Row="5" Grid.Column="0" Text="Cash from sales:" HorizontalAlignment="Right"/>
                    <TextBlock Grid.Row="5" Grid.Column="1" x:Name="CashInText" TextAlignment="Right"
                               Text="{Binding CashIn}" />

                    <TextBlock Grid.Row="6" Grid.Column="0" Text="Cash refunds:" HorizontalAlignment="Right"/>
                    <TextBlock Grid.Row="6" Grid.Column="1" x:Name="CashOutText" TextAlignment="Right"
                               Text="{Binding CashOut}" />

                    <Border Grid.Row="7" Grid.ColumnSpan="2" BorderBrush="#EEE" BorderThickness="0,1,0,0" Margin="0,6,0,6"/>

                    <TextBlock Grid.Row="8" Grid.Column="0" Text="Expected cash in till:" HorizontalAlignment="Right" FontWeight="SemiBold"/>
                    <TextBlock Grid.Row="8" Grid.Column="1" x:Name="ExpectedCashText" TextAlignment="Right" FontWeight="Bold"
                               Text="{Binding ExpectedCash}" />

                    <TextBlock Grid.Row="9" Grid.Column="0" Text="Card sales:" HorizontalAlignment="Right"/>
                    <TextBlock Grid.Row="9" Grid.Column="1" x:Name="CardInText" TextAlignment="Right"
                               Text="{Binding CardIn}" />

                    <TextBlock Grid.Row="10" Grid.Column="0" Text="Card refunds:" HorizontalAlignment="Right"/>
                    <TextBlock Grid.Row="10" Grid.Column="1" x:Name="CardOutText" TextAlignment="Right"
                               Text="{Binding CardOut}" />
                </Grid>
            </GroupBox>

            <!-- Right pane: Activity -->
            <GroupBox Header="Activity" Grid.Column="1" Margin="8,0,0,0">
                <Grid Margin="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="8"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="8"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="8"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="180" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Sales count:" HorizontalAlignment="Right"/>
                    <TextBlock Grid.Row="0" Grid.Column="1" x:Name="SalesCountText" TextAlignment="Right"
                               Text="{Binding SalesCount}" />

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Return count:" HorizontalAlignment="Right"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" x:Name="ReturnsCountText" TextAlignment="Right"
                               Text="{Binding ReturnsCount}" />

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Total invoices:" HorizontalAlignment="Right"/>
                    <TextBlock Grid.Row="2" Grid.Column="1" x:Name="DocsCountText" TextAlignment="Right"
                               Text="{Binding DocsCount}" />

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Last transaction:" HorizontalAlignment="Right"/>
                    <TextBlock Grid.Row="3" Grid.Column="1" x:Name="LastTxText" TextAlignment="Right"
                               Text="{Binding LastTx}" />

                    <Border Grid.Row="4" Grid.ColumnSpan="2" BorderBrush="#EEE" BorderThickness="0,1,0,0" Margin="0,6,0,6"/>

                    <TextBlock Grid.Row="5" Grid.Column="0" Text="Items sold (qty):" HorizontalAlignment="Right"/>
                    <TextBlock Grid.Row="5" Grid.Column="1" x:Name="ItemsSoldQtyText" TextAlignment="Right"
                               Text="{Binding ItemsSoldQty}" />

                    <TextBlock Grid.Row="6" Grid.Column="0" Text="Items returned (qty):" HorizontalAlignment="Right"/>
                    <TextBlock Grid.Row="6" Grid.Column="1" x:Name="ItemsReturnedQtyText" TextAlignment="Right"
                               Text="{Binding ItemsReturnedQty}" />

                    <TextBlock Grid.Row="7" Grid.Column="0" Text="Net items (qty):" HorizontalAlignment="Right"/>
                    <TextBlock Grid.Row="7" Grid.Column="1" x:Name="ItemsNetQtyText" TextAlignment="Right"
                               Text="{Binding ItemsNetQty}" />

                    <Border Grid.Row="8" Grid.ColumnSpan="2" BorderBrush="#EEE" BorderThickness="0,1,0,0" Margin="0,6,0,6"/>

                    <TextBlock Grid.Row="9"  Grid.Column="0" Text="Tax collected:" HorizontalAlignment="Right"/>
                    <TextBlock Grid.Row="9"  Grid.Column="1" x:Name="TaxCollectedText" TextAlignment="Right"
                               Text="{Binding TaxCollected}" />

                    <TextBlock Grid.Row="10" Grid.Column="0" Text="Tax refunded:" HorizontalAlignment="Right"/>
                    <TextBlock Grid.Row="10" Grid.Column="1" x:Name="TaxRefundedText" TextAlignment="Right"
                               Text="{Binding TaxRefunded}" />

                    <Border Grid.Row="11" Grid.ColumnSpan="2" BorderBrush="#EEE" BorderThickness="0,1,0,0" Margin="0,6,0,6"/>

                    <TextBlock Grid.Row="12" Grid.Column="0" Text="Sale amendments:" HorizontalAlignment="Right"/>
                    <TextBlock Grid.Row="12" Grid.Column="1" x:Name="SalesAmendmentsText" TextAlignment="Right"
                               Text="{Binding SalesAmendments}" />

                    <TextBlock Grid.Row="13" Grid.Column="0" Text="Return amendments:" HorizontalAlignment="Right"/>
                    <TextBlock Grid.Row="13" Grid.Column="1" x:Name="ReturnAmendmentsText" TextAlignment="Right"
                               Text="{Binding ReturnAmendments}" />

                    <TextBlock Grid.Row="14" Grid.Column="0" Text="Voided invoices:" HorizontalAlignment="Right"/>
                    <TextBlock Grid.Row="14" Grid.Column="1" x:Name="VoidsCountText" TextAlignment="Right"
                               Text="{Binding VoidsCount}" />
                </Grid>
            </GroupBox>
        </Grid>

        <!-- Footer -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Refresh (F5)" Width="120" Height="32" Margin="0,0,8,0"
                    Command="{Binding LoadCmd}"/>
            <Button Content="Close (Esc)" Width="110" Height="32" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
