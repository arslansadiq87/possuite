<Window x:Class="Pos.Client.Wpf.Windows.Accounting.OpeningBalanceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Opening Balance" Height="560" Width="900"
        WindowStartupLocation="CenterOwner">
    <DockPanel Margin="12">
        <!-- Header -->
        <Grid DockPanel.Dock="Top" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" Grid.Column="0" VerticalAlignment="Center">
                <TextBlock Text="As of:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                <DatePicker SelectedDate="{Binding AsOfDate}" Width="140"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Grid.Column="1" Margin="12,0,0,0" VerticalAlignment="Center">
                <TextBlock Text="Outlet Id:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                <TextBox Width="120" Text="{Binding OutletId}"/>
            </StackPanel>

            <TextBox Grid.Column="2" Height="28" Margin="12,0,0,0"
                     Text="{Binding Memo}"/>

            <StackPanel Grid.Column="3" Orientation="Horizontal" HorizontalAlignment="Right" >
                <Button Content="Add Line" Width="90" Margin="0,0,8,0"
                        Command="{Binding AddLineCommand}"/>
                <Button Content="Save Draft" Width="110" Margin="0,0,8,0"
                        Command="{Binding SaveCommand}"/>
                <Button Content="Post" Width="90"
                        Command="{Binding PostCommand}"/>
            </StackPanel>
        </Grid>

        <!-- Grid -->
        <DataGrid ItemsSource="{Binding Lines}"
                  AutoGenerateColumns="False" CanUserAddRows="False"
                  HeadersVisibility="Column" GridLinesVisibility="All">
            <DataGrid.Columns>
                <!-- Account -->
                <DataGridTemplateColumn Header="Account" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <DockPanel>
                                <ComboBox Width="360"
                                          ItemsSource="{Binding DataContext.Accounts, RelativeSource={RelativeSource AncestorType=Window}}"
                                          SelectedValuePath="Id"
                                          DisplayMemberPath="Name"
                                          SelectedValue="{Binding AccountId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{Binding Code}" Width="90"/>
                                                <TextBlock Text="{Binding Name}"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>
                            </DockPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Code" Width="120"
                                    Binding="{Binding AccountCode, Mode=OneWay}"/>
                <DataGridTextColumn Header="Name" Width="220"
                                    Binding="{Binding AccountName, Mode=OneWay}"/>

                <DataGridTextColumn Header="Debit" Width="120"
                                    Binding="{Binding Debit, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, StringFormat=N2}"/>
                <DataGridTextColumn Header="Credit" Width="120"
                                    Binding="{Binding Credit, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, StringFormat=N2}"/>

                <DataGridTemplateColumn Header="" Width="60">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Del" Width="44"
                                    Command="{Binding DataContext.RemoveLineCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding .}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Totals -->
        <Grid DockPanel.Dock="Bottom" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="140"/>
                <ColumnDefinition Width="140"/>
                <ColumnDefinition Width="140"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{Binding IsPosted, StringFormat=Posted: {0}}"
                       HorizontalAlignment="Left" VerticalAlignment="Center"/>
            <TextBlock Grid.Column="1" Text="{Binding TotalDebit, StringFormat=Total Dr: {0:N2}}" HorizontalAlignment="Right"/>
            <TextBlock Grid.Column="2" Text="{Binding TotalCredit, StringFormat=Total Cr: {0:N2}}" HorizontalAlignment="Right"/>
            <TextBlock Grid.Column="3" Text="{Binding Difference, StringFormat=Diff (Dr - Cr): {0:N2}}"
                       HorizontalAlignment="Right" FontWeight="Bold"/>
        </Grid>
    </DockPanel>
</Window>
