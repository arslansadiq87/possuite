<Window x:Class="Pos.Client.Wpf.Windows.Accounting.AccountEditorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding TitleText}" Height="240" Width="520"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Parent:" Width="80" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding ParentPath}" FontWeight="SemiBold"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Name:" Width="80" VerticalAlignment="Center"/>
            <TextBox Text="{Binding AccountName, UpdateSourceTrigger=PropertyChanged}" Width="360"/>
        </StackPanel>

        <CheckBox Grid.Row="2" Content="Header (no posting)"
              IsChecked="{Binding IsHeader}" />

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Cancel" Width="90" Margin="0,0,10,0" IsCancel="True"/>
            <Button Content="Save" Width="110" IsDefault="True" Click="OnSave"/>
        </StackPanel>
    </Grid>
</Window>
