<UserControl x:Class="Pos.Client.Wpf.Windows.Accounting.PurchaserLedgerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Controls="clr-namespace:Pos.Client.Wpf.Controls"
             FocusManager.FocusedElement="{Binding ElementName=SupplierSearch}">

    <!-- Root grid: Row 0 = toolbar, Row 1 = data grid, Row 2 = totals -->
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Toolbar (Row 0) -->
        <ToolBar Grid.Row="0">
            <TextBlock Text="From:" VerticalAlignment="Center"/>
            <DatePicker SelectedDate="{Binding From, Mode=TwoWay}" Margin="6,0,8,0"/>

            <TextBlock Text="To:" VerticalAlignment="Center"/>
            <DatePicker SelectedDate="{Binding To, Mode=TwoWay}" Margin="6,0,8,0"/>

            <TextBlock Text="Supplier:" VerticalAlignment="Center"/>

            <Controls:CustomerSearchBox
                x:Name="SupplierSearch"
                Width="260"
                Mode="Suppliers"
                CustomerPicked="SupplierSearch_CustomerPicked" />

            <Button Content="Clear" Click="ClearSupplier_Click" Margin="6,0,10,0"/>
            <Button Content="Refresh" Command="{Binding RefreshCommand}"/>
            <Separator/>
            <Button Content="Print" Click="OnPrintClick"/>
        </ToolBar>

        <!-- Data grid (Row 1) -->
        <DataGrid Grid.Row="1"
                  x:Name="Grid"
                  ItemsSource="{Binding Rows}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  Margin="0,6,0,6"
                  EnableRowVirtualization="True"
                  EnableColumnVirtualization="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Date" Binding="{Binding TsUtc}" Width="140"/>
                <DataGridTextColumn Header="Doc No" Binding="{Binding DocNo}" Width="120"/>
                <DataGridTextColumn Header="Supplier" Binding="{Binding Supplier}" Width="*"/>
                <DataGridTextColumn Header="Total" Binding="{Binding GrandTotal, StringFormat=N2}" Width="120"/>
                <DataGridTextColumn Header="Paid" Binding="{Binding Paid, StringFormat=N2}" Width="120"/>
                <DataGridTextColumn Header="Due" Binding="{Binding Due, StringFormat=N2}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Totals footer (Row 2) -->
        <StatusBar Grid.Row="2" HorizontalAlignment="Right" FontWeight="Bold">
            <StatusBarItem>
                <TextBlock Text="Totals:"/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock>
                    <Run Text="Grand: "/>
                    <Run Text="{Binding TotalGrand, StringFormat=N2}"/>
                </TextBlock>
            </StatusBarItem>
            <StatusBarItem>
                <TextBlock>
                    <Run Text="Paid: "/>
                    <Run Text="{Binding TotalPaid, StringFormat=N2}"/>
                </TextBlock>
            </StatusBarItem>
            <StatusBarItem>
                <TextBlock>
                    <Run Text="Due: "/>
                    <Run Text="{Binding TotalDue, StringFormat=N2}"/>
                </TextBlock>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</UserControl>
