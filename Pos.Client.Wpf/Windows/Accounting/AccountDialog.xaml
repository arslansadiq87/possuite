<Window x:Class="Pos.Client.Wpf.Windows.Accounting.AccountDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Account" Height="360" Width="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Code -->
            <RowDefinition Height="Auto"/>
            <!-- Name -->
            <RowDefinition Height="Auto"/>
            <!-- Parent -->
            <RowDefinition Height="Auto"/>
            <!-- Type -->
            <RowDefinition Height="Auto"/>
            <!-- Normal -->
            <RowDefinition Height="Auto"/>
            <!-- Active -->
            <RowDefinition Height="Auto"/>
            <!-- Outlet-Scoped -->
            <RowDefinition Height="Auto"/>
            <!-- Buttons -->
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="130"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Code -->
        <TextBlock Text="Code" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox Text="{Binding Value.Code}" Grid.Row="0" Grid.Column="1" Margin="6"/>

        <!-- Name -->
        <TextBlock Text="Name" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox Text="{Binding Value.Name}" Grid.Row="1" Grid.Column="1" Margin="6"/>

        <!-- Parent -->
        <TextBlock Text="Parent" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center"/>
        <ComboBox Grid.Row="2" Grid.Column="1" Margin="6"
                  ItemsSource="{Binding ParentChoices}"
                  DisplayMemberPath="Name"
                  SelectedItem="{Binding SelectedParent}"
                  IsEditable="False" />

        <!-- Type -->
        <TextBlock Text="Type" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center"/>
        <ComboBox Grid.Row="3" Grid.Column="1" Margin="6"
                  ItemsSource="{Binding Types}"
                  SelectedItem="{Binding Value.Type}"/>

        <!-- Normal -->
        <TextBlock Text="Normal" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center"/>
        <ComboBox Grid.Row="4" Grid.Column="1" Margin="6"
                  ItemsSource="{Binding Normals}"
                  SelectedItem="{Binding Value.Normal}"/>

        <!-- Active -->
        <CheckBox Content="Active" Grid.Row="5" Grid.ColumnSpan="2" Margin="0,8,0,0"
                  IsChecked="{Binding Value.IsActive}"/>

        <!-- Outlet-Scoped -->
        <CheckBox Content="Outlet-Scoped" Grid.Row="6" Grid.ColumnSpan="2"
                  IsChecked="{Binding Value.IsOutletScoped}"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="7" Grid.ColumnSpan="2"
                    Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Width="90" Margin="0,0,8,0" Content="OK" IsDefault="True" Click="Ok_Click"/>
            <Button Width="90" Content="Cancel" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
