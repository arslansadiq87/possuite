<Window x:Class="Pos.Client.Wpf.Windows.Shell.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sign In" Height="260" Width="360"
        WindowStartupLocation="CenterScreen"
        Loaded="Window_Loaded">

    <Grid Margin="16" KeyboardNavigation.TabNavigation="Cycle">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Username" Grid.Row="0" Margin="0,0,0,4"/>
        <TextBox x:Name="UserBox"
                 Grid.Row="1" Height="28" Margin="0,0,0,12"
                 TabIndex="0"
                 KeyDown="UserBox_KeyDown"/>

        <TextBlock Text="Password" Grid.Row="2" Margin="0,0,0,4"/>
        <PasswordBox x:Name="PassBox"
                     Grid.Row="3" Height="28" Margin="0,0,0,12"
                     TabIndex="1"
                     KeyDown="PassBox_KeyDown"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="LoginBtn"
                    Content="Login" Width="100" Height="32"
                    IsDefault="True"
                    Click="Login_Click"/>
        </StackPanel>
    </Grid>
</Window>
