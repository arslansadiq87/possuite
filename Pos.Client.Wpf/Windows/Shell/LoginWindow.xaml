<Window x:Class="Pos.Client.Wpf.Windows.Shell.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sign In" Height="260" Width="360"
        WindowStartupLocation="CenterScreen"
        Loaded="Window_Loaded">

    <Grid Margin="16" KeyboardNavigation.TabNavigation="Cycle">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Username" Grid.Row="0" Margin="0,0,0,4" Style="{StaticResource ModernTextBlock}"/>
        <TextBox x:Name="UserBox"
                 Grid.Row="1" Margin="0,0,0,12"
                 TabIndex="0"
                 KeyDown="UserBox_KeyDown" Style="{StaticResource Txt.Outlined}"/>

        <TextBlock Text="Password" Grid.Row="2" Margin="0,0,0,4" Style="{StaticResource ModernTextBlock}"/>
        <PasswordBox x:Name="PassBox"
                     Grid.Row="3" Margin="0,0,0,12"
                     TabIndex="1"
                     KeyDown="PassBox_KeyDown" Style="{StaticResource Txt.Password}"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="LoginBtn"
                    Content="Login" Width="100" Height="32"
                    IsDefault="True"
                    Click="Login_Click" Style="{StaticResource Btn.GlobalPrimary}"/>
        </StackPanel>
    </Grid>
</Window>
