<Window x:Class="Pos.Client.Wpf.Windows.Purchases.SupplierQuickDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="New Supplier" Width="420" Height="360"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Name" FontWeight="SemiBold"/>
            <TextBox x:Name="NameBox" Margin="0,4,0,8"/>

            <TextBlock Text="Phone" FontWeight="SemiBold"/>
            <TextBox x:Name="PhoneBox" Margin="0,4,0,8"/>

            <TextBlock Text="Email" FontWeight="SemiBold"/>
            <TextBox x:Name="EmailBox" Margin="0,4,0,8"/>

            <TextBlock Text="Address" FontWeight="SemiBold"/>
            <TextBox x:Name="Address1Box" Margin="0,4,0,8"/>

            <Grid Margin="0,4,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="160"/>
                    <ColumnDefinition Width="160"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="City" FontWeight="SemiBold"/>
                    <TextBox x:Name="CityBox" Margin="0,4,0,0"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Margin="8,0,0,0">
                    <TextBlock Text="Country" FontWeight="SemiBold"/>
                    <TextBox x:Name="CountryBox" Margin="0,4,0,0"/>
                </StackPanel>
            </Grid>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Save" Width="100" Margin="0,0,8,0" Click="Save_Click"/>
            <Button Content="Cancel" Width="100" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
