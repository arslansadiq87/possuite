<UserControl x:Class="Pos.Client.Wpf.Windows.Settings.ReceiptBuilderPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" d:DesignWidth="1000" d:DesignHeight="680">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <!-- Left: Template editors per DocType -->
        <TabControl Grid.Column="0" Margin="0,0,12,0"
                SelectedIndex="{Binding SelectedTabIndex}">
            <TabItem Header="Sale">
                <ScrollViewer>
                    <StackPanel Margin="8">
                        <!-- Outlet Display Name -->
                        <TextBlock Text="Outlet Display Name" FontWeight="SemiBold" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding SaleTemplate.OutletDisplayName}" />

                        <!-- Paper Width -->
                        <TextBlock Text="Paper Width (mm)" FontWeight="SemiBold" Margin="0,12,0,4"/>
                        <ComboBox SelectedValue="{Binding SaleTemplate.PaperWidthMm}">
                            <sys:Int32>58</sys:Int32>
                            <sys:Int32>80</sys:Int32>
                        </ComboBox>

                        <!-- Show Customer/Cashier -->
                        <CheckBox Content="Show Customer" IsChecked="{Binding SaleTemplate.ShowCustomerOnReceipt}" Margin="0,12,0,0"/>
                        <CheckBox Content="Show Cashier"  IsChecked="{Binding SaleTemplate.ShowCashierOnReceipt}"/>

                        <!-- Footer -->
                        <TextBlock Text="Footer" FontWeight="SemiBold" Margin="0,12,0,4"/>
                        <TextBox AcceptsReturn="True" Height="80" Text="{Binding SaleTemplate.FooterText}"/>

                        <!-- Actions -->
                        <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                            <Button Content="Save"    Command="{Binding SaveSaleCommand}"    Margin="0,0,8,0"/>
                            <Button Content="Preview" Command="{Binding PreviewSaleCommand}" Margin="0,0,8,0"/>
                            <Button Content="Print"   Command="{Binding PrintSaleCommand}"/>
                        </StackPanel>
                    </StackPanel>

                </ScrollViewer>
            </TabItem>

            <TabItem Header="Sale Return">
                <!-- same as above, bound to SaleReturnTemplate -->
                <!-- ... -->
            </TabItem>

            <TabItem Header="Voucher">
                <!-- bound to VoucherTemplate -->
                <!-- ... -->
            </TabItem>

            <TabItem Header="Z-Report">
                <!-- bound to ZReportTemplate -->
                <!-- ... -->
            </TabItem>
        </TabControl>

        <!-- Right: Live text preview (monospace) -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <TextBlock Text="Receipt Preview" FontWeight="Bold" FontSize="16"/>
            <ScrollViewer Grid.Row="1" Margin="0,8,0,0" Background="#111" Padding="12">
                <TextBlock Text="{Binding PreviewText}" FontFamily="Consolas" Foreground="White" />
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
