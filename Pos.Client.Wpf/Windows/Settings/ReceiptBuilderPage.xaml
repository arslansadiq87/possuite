<UserControl x:Class="Pos.Client.Wpf.Windows.Settings.ReceiptBuilderPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:Pos.Client.Wpf.Converters"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             Width="1000" MaxWidth="1200">

    <UserControl.Resources>
        <conv:NegateBooleanConverter x:Key="NegateBool"/>
        <conv:BytesToBitmapConverter x:Key="BytesToBitmap"/>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Header -->
            <RowDefinition Height="Auto"/>
            <!-- Scope -->
            <RowDefinition Height="*"/>
            <!-- Main -->
            <RowDefinition Height="Auto"/>
            <!-- Actions -->
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <DockPanel Grid.Row="0" LastChildFill="False" Margin="0,0,0,16">
            <StackPanel Orientation="Vertical" DockPanel.Dock="Left">
                <TextBlock Text="Receipt Builder"
                           FontSize="18"
                           FontWeight="SemiBold"/>
                <TextBlock Text="Configure sales receipt layout per outlet: paper width, logo, row/totals visibility, QR/barcode, NTN/FBR. Printer and footer come from Invoice Settings."
                           FontSize="12"
                           Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                           Margin="0,2,0,0"/>
            </StackPanel>
        </DockPanel>

        <!-- SCOPE SELECTOR -->
        <Border Grid.Row="1"
                CornerRadius="4"
                Padding="0,6"
                Margin="0,0,0,16"
                Background="#0F000000"
                BorderBrush="#22000000"
                BorderThickness="1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Scope:"
                           VerticalAlignment="Center"
                           Margin="8,0,16,0"
                           FontWeight="SemiBold"/>

                <RadioButton Content="Global"
                             Margin="0,0,8,0"
                             VerticalAlignment="Center"
                             IsChecked="{Binding IsGlobal}"
                             GroupName="Scope"
                             IsEnabled="{Binding CanEditGlobal}"/>

                <RadioButton Content="Specific outlet"
                             Margin="0,0,8,0"
                             VerticalAlignment="Center"
                             IsChecked="{Binding IsGlobal, Converter={StaticResource NegateBool}}"
                             GroupName="Scope"
                             IsEnabled="{Binding CanEditGlobal}"/>

                <ComboBox Width="220" Height="26"
                          VerticalAlignment="Center"
                          VerticalContentAlignment="Center"
                          ItemsSource="{Binding OutletChoices}"
                          SelectedItem="{Binding SelectedOutlet}"
                          DisplayMemberPath="Name"
                          IsEnabled="{Binding CanEditGlobal}"
                          Margin="8,0,0,0"/>
            </StackPanel>
        </Border>

        <!-- MAIN CONTENT -->
        <Grid Grid.Row="2" IsEnabled="{Binding CanEdit}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- LEFT CARD: Controls -->
            <Border Grid.Column="0"
                    Background="#06000000"
                    BorderBrush="#22000000"
                    BorderThickness="1"
                    CornerRadius="4"
                    Padding="14"
                    Margin="0,0,12,0">
                <StackPanel>

                    <!-- Printer & Paper width -->
                    <TextBlock Text="Printer &amp; paper"
                               FontWeight="SemiBold"
                               Margin="0,0,0,8"/>
                    <Grid Margin="0,0,12,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0"
                                   Text="Printer"
                                   VerticalAlignment="Center"
                                   Margin="0,0,8,0"/>
                        <TextBlock Grid.Column="1"
                                   VerticalAlignment="Center"
                                   FontWeight="SemiBold"
                                   Text="{Binding InvoicePrinterName}"/>
                    </Grid>

                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0"
                                   Text="Paper width"
                                   VerticalAlignment="Center"
                                   Margin="0,0,8,0"/>
                        <ComboBox Grid.Column="1"
                                  Width="140" Height="26"
                                  VerticalContentAlignment="Center"
                                  SelectedValue="{Binding SaleTemplate.PaperWidthMm, Mode=TwoWay}"
                                  SelectedValuePath="Tag">
                            <ComboBoxItem Content="58 mm" Tag="58"/>
                            <ComboBoxItem Content="80 mm" Tag="80"/>
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="SelectionChanged">
                                    <i:InvokeCommandAction Command="{Binding PreviewSaleCommand}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </ComboBox>
                    </Grid>

                    <Separator Margin="0,0,0,10"/>

                    <!-- Logo -->
                    <TextBlock Text="Logo"
                               FontWeight="SemiBold"
                               Margin="0,0,0,8"/>
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Preview -->
                        <Border Grid.Column="0"
                                BorderBrush="#33000000"
                                BorderThickness="1"
                                CornerRadius="4"
                                Padding="6"
                                Height="100"
                                Background="#05000000"
                                Margin="0,0,8,0">
                            <Image Stretch="Uniform"
                                   Source="{Binding IdentityLogoBytes, Converter={StaticResource BytesToBitmap}}"/>
                        </Border>

                        <!-- Controls -->
                        <StackPanel Grid.Column="1" Margin="8,0,0,0">
                            <CheckBox Content="Show logo"
                                      IsChecked="{Binding SaleTemplate.ShowLogoOnReceipt}">
                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="Checked">
                                        <i:InvokeCommandAction Command="{Binding PreviewSaleCommand}"/>
                                    </i:EventTrigger>
                                    <i:EventTrigger EventName="Unchecked">
                                        <i:InvokeCommandAction Command="{Binding PreviewSaleCommand}"/>
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>
                            </CheckBox>

                            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                <TextBlock Text="Max width (px):" VerticalAlignment="Center"/>
                                <TextBox Width="90" Height="26" Margin="8,0,0,0"
                                         VerticalContentAlignment="Center"
                                         Text="{Binding SaleTemplate.LogoMaxWidthPx, UpdateSourceTrigger=PropertyChanged}">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="TextChanged">
                                            <i:InvokeCommandAction Command="{Binding PreviewSaleCommand}"/>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </TextBox>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                <TextBlock Text="Alignment:" VerticalAlignment="Center"/>
                                <ComboBox Width="140" Height="26" Margin="8,0,0,0"
                                          VerticalContentAlignment="Center"
                                          SelectedItem="{Binding SaleTemplate.LogoAlignment, UpdateSourceTrigger=PropertyChanged}">
                                    <ComboBoxItem Content="Left"/>
                                    <ComboBoxItem Content="Center"/>
                                    <ComboBoxItem Content="Right"/>
                                </ComboBox>
                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="SelectionChanged">
                                        <i:InvokeCommandAction Command="{Binding PreviewSaleCommand}"/>
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>
                            </StackPanel>
                        </StackPanel>
                    </Grid>

                    <Separator Margin="0,0,0,10"/>

                    <!-- Row visibility -->
                    <TextBlock Text="Row visibility (items)"
                               FontWeight="SemiBold"
                               Margin="0,0,0,8"/>
                    <WrapPanel Margin="0,0,0,12">
                        <CheckBox Content="Product name"   IsChecked="{Binding SaleTemplate.RowShowProductName}"  Margin="0,0,16,6">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Checked">
                                    <i:InvokeCommandAction Command="{Binding PreviewSaleCommand}"/>
                                </i:EventTrigger>
                                <i:EventTrigger EventName="Unchecked">
                                    <i:InvokeCommandAction Command="{Binding PreviewSaleCommand}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </CheckBox>
                        <CheckBox Content="Product SKU"    IsChecked="{Binding SaleTemplate.RowShowProductSku}"   Margin="0,0,16,6">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Checked">
                                    <i:InvokeCommandAction Command="{Binding PreviewSaleCommand}"/>
                                </i:EventTrigger>
                                <i:EventTrigger EventName="Unchecked">
                                    <i:InvokeCommandAction Command="{Binding PreviewSaleCommand}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </CheckBox>
                        <CheckBox Content="Quantity"       IsChecked="{Binding SaleTemplate.RowShowQty}"          Margin="0,0,16,6">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Checked">
                                    <i:InvokeCommandAction Command="{Binding PreviewSaleCommand}"/>
                                </i:EventTrigger>
                                <i:EventTrigger EventName="Unchecked">
                                    <i:InvokeCommandAction Command="{Binding PreviewSaleCommand}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </CheckBox>
                        <CheckBox Content="Unit price"     IsChecked="{Binding SaleTemplate.RowShowUnitPrice}"    Margin="0,0,16,6">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Checked">
                                    <i:InvokeCommandAction Command="{Binding PreviewSaleCommand}"/>
                                </i:EventTrigger>
                                <i:EventTrigger EventName="Unchecked">
                                    <i:InvokeCommandAction Command="{Binding PreviewSaleCommand}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </CheckBox>
                        <CheckBox Content="Line discount"  IsChecked="{Binding SaleTemplate.RowShowLineDiscount}" Margin="0,0,16,6">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Checked">
                                    <i:InvokeCommandAction Command="{Binding PreviewSaleCommand}"/>
                                </i:EventTrigger>
                                <i:EventTrigger EventName="Unchecked">
                                    <i:InvokeCommandAction Command="{Binding PreviewSaleCommand}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </CheckBox>
                        <CheckBox Content="Line total"     IsChecked="{Binding SaleTemplate.RowShowLineTotal}"    Margin="0,0,16,6">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Checked">
                                    <i:InvokeCommandAction Command="{Binding PreviewSaleCommand}"/>
                                </i:EventTrigger>
                                <i:EventTrigger EventName="Unchecked">
                                    <i:InvokeCommandAction Command="{Binding PreviewSaleCommand}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </CheckBox>
                    </WrapPanel>

                    <Separator Margin="0,0,0,10"/>

                    <!-- Totals -->
                    <TextBlock Text="Totals"
                               FontWeight="SemiBold"
                               Margin="0,0,0,8"/>
                    <WrapPanel Margin="0,0,0,12">
                        <CheckBox Content="Taxes"            IsChecked="{Binding SaleTemplate.TotalsShowTaxes}"          Margin="0,0,16,6">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Checked">
                                    <i:InvokeCommandAction Command="{Binding PreviewSaleCommand}"/>
                                </i:EventTrigger>
                                <i:EventTrigger EventName="Unchecked">
                                    <i:InvokeCommandAction Command="{Binding PreviewSaleCommand}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </CheckBox>
                        <CheckBox Content="Discounts"        IsChecked="{Binding SaleTemplate.TotalsShowDiscounts}"      Margin="0,0,16,6">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Checked">
                                    <i:InvokeCommandAction Command="{Binding PreviewSaleCommand}"/>
                                </i:EventTrigger>
                                <i:EventTrigger EventName="Unchecked">
                                    <i:InvokeCommandAction Command="{Binding PreviewSaleCommand}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </CheckBox>
                        <CheckBox Content="Other expenses"   IsChecked="{Binding SaleTemplate.TotalsShowOtherExpenses}"  Margin="0,0,16,6">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Checked">
                                    <i:InvokeCommandAction Command="{Binding PreviewSaleCommand}"/>
                                </i:EventTrigger>
                                <i:EventTrigger EventName="Unchecked">
                                    <i:InvokeCommandAction Command="{Binding PreviewSaleCommand}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </CheckBox>
                        <CheckBox Content="Grand total"      IsChecked="{Binding SaleTemplate.TotalsShowGrandTotal}"     Margin="0,0,16,6">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Checked">
                                    <i:InvokeCommandAction Command="{Binding PreviewSaleCommand}"/>
                                </i:EventTrigger>
                                <i:EventTrigger EventName="Unchecked">
                                    <i:InvokeCommandAction Command="{Binding PreviewSaleCommand}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </CheckBox>
                        <CheckBox Content="Payment received" IsChecked="{Binding SaleTemplate.TotalsShowPaymentRecv}"    Margin="0,0,16,6">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Checked">
                                    <i:InvokeCommandAction Command="{Binding PreviewSaleCommand}"/>
                                </i:EventTrigger>
                                <i:EventTrigger EventName="Unchecked">
                                    <i:InvokeCommandAction Command="{Binding PreviewSaleCommand}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </CheckBox>
                        <CheckBox Content="Balance"          IsChecked="{Binding SaleTemplate.TotalsShowBalance}"        Margin="0,0,16,6">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Checked">
                                    <i:InvokeCommandAction Command="{Binding PreviewSaleCommand}"/>
                                </i:EventTrigger>
                                <i:EventTrigger EventName="Unchecked">
                                    <i:InvokeCommandAction Command="{Binding PreviewSaleCommand}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </CheckBox>
                    </WrapPanel>

                    <Separator Margin="0,0,0,10"/>

                    <!-- Common (Sales) -->
                    <TextBlock Text="Common (sales)"
                               FontWeight="SemiBold"
                               Margin="0,0,0,8"/>
                    <StackPanel>
                        <CheckBox Content="Show QR (e-receipt/other)" IsChecked="{Binding SaleTemplate.ShowQr}">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Checked">
                                    <i:InvokeCommandAction Command="{Binding PreviewSaleCommand}"/>
                                </i:EventTrigger>
                                <i:EventTrigger EventName="Unchecked">
                                    <i:InvokeCommandAction Command="{Binding PreviewSaleCommand}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </CheckBox>

                        <CheckBox Content="Show customer" IsChecked="{Binding SaleTemplate.ShowCustomerOnReceipt}">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Checked">
                                    <i:InvokeCommandAction Command="{Binding PreviewSaleCommand}"/>
                                </i:EventTrigger>
                                <i:EventTrigger EventName="Unchecked">
                                    <i:InvokeCommandAction Command="{Binding PreviewSaleCommand}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </CheckBox>

                        <CheckBox Content="Show cashier" IsChecked="{Binding SaleTemplate.ShowCashierOnReceipt}">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Checked">
                                    <i:InvokeCommandAction Command="{Binding PreviewSaleCommand}"/>
                                </i:EventTrigger>
                                <i:EventTrigger EventName="Unchecked">
                                    <i:InvokeCommandAction Command="{Binding PreviewSaleCommand}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </CheckBox>

                        <CheckBox Content="Print barcode" IsChecked="{Binding SaleTemplate.PrintBarcodeOnReceipt}">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Checked">
                                    <i:InvokeCommandAction Command="{Binding PreviewSaleCommand}"/>
                                </i:EventTrigger>
                                <i:EventTrigger EventName="Unchecked">
                                    <i:InvokeCommandAction Command="{Binding PreviewSaleCommand}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </CheckBox>

                        <CheckBox Content="Show NTN number"
                                  IsChecked="{Binding SaleTemplate.ShowNtnOnReceipt}"
                                  IsEnabled="{Binding RuntimeHasNtn}"
                                  ToolTip="Disabled because NTN is not set in General Settings">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Checked">
                                    <i:InvokeCommandAction Command="{Binding PreviewSaleCommand}"/>
                                </i:EventTrigger>
                                <i:EventTrigger EventName="Unchecked">
                                    <i:InvokeCommandAction Command="{Binding PreviewSaleCommand}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </CheckBox>

                        <CheckBox Content="Show FBR data (QR &amp; logo)"
                                  IsChecked="{Binding SaleTemplate.ShowFbrOnReceipt}"
                                  IsEnabled="{Binding RuntimeEnableFbr}"
                                  ToolTip="Disabled because FBR mode is not enabled in General Settings">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Checked">
                                    <i:InvokeCommandAction Command="{Binding PreviewSaleCommand}"/>
                                </i:EventTrigger>
                                <i:EventTrigger EventName="Unchecked">
                                    <i:InvokeCommandAction Command="{Binding PreviewSaleCommand}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </CheckBox>
                    </StackPanel>

                </StackPanel>
            </Border>

            <!-- RIGHT CARD: Live Preview -->
            <Border Grid.Column="1"
                    Background="#06000000"
                    BorderBrush="#22000000"
                    BorderThickness="1"
                    CornerRadius="4"
                    Padding="4">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Receipt preview"
                               FontWeight="SemiBold"
                               Margin="0,0,0,8"/>

                    <ScrollViewer Grid.Row="1"
                                  Margin="0,4,0,0"
                                  Background="#111"
                                  Padding="6">
                        <TextBlock Text="{Binding PreviewText}"
                                   FontFamily="Consolas"
                                   Foreground="White"
                                   TextWrapping="Wrap"/>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>

        <!-- ACTIONS -->
        <DockPanel Grid.Row="3" Margin="0,16,0,0">
            <TextBlock DockPanel.Dock="Left"
                       VerticalAlignment="Center"
                       FontSize="11"
                       Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                       Text="Printer and footer come from Invoice Settings. Layout is saved per outlet (or globally)."/>
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                <Button Content="Save" Height="26" MinWidth="90"
                        Command="{Binding SaveSaleCommand}"
                        Margin="0,0,8,0"/>
                <Button Content="Print Test" Height="26" MinWidth="90"
                        Command="{Binding PrintSaleCommand}"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</UserControl>
