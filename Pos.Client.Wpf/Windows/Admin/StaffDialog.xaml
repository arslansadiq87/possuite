<Window x:Class="Pos.Client.Wpf.Windows.Admin.StaffDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding DialogTitle}"
        Height="380" Width="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        FontSize="13">
    <DockPanel>
        <Border Background="#f7f7fb" Padding="14" DockPanel.Dock="Top">
            <TextBlock Text="{Binding DialogTitle}" FontSize="16" FontWeight="SemiBold"/>
        </Border>

        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Code" Grid.Row="0" Grid.Column="0" Margin="0,6,8,6" VerticalAlignment="Center"/>
                <TextBox x:Name="CodeBox" Grid.Row="0" Grid.Column="1" Margin="0,4,0,4" />

                <TextBlock Text="Full Name" Grid.Row="1" Grid.Column="0" Margin="0,6,8,6" VerticalAlignment="Center"/>
                <TextBox x:Name="NameBox" Grid.Row="1" Grid.Column="1" Margin="0,4,0,4"/>

                <TextBlock Text="Join Date" Grid.Row="2" Grid.Column="0" Margin="0,6,8,6" VerticalAlignment="Center"/>
                <DatePicker x:Name="JoinDatePicker" Grid.Row="2" Grid.Column="1" Margin="0,4,0,4"/>

                <TextBlock Text="Basic Salary" Grid.Row="3" Grid.Column="0" Margin="0,6,8,6" VerticalAlignment="Center"/>
                <TextBox x:Name="SalaryBox" Grid.Row="3" Grid.Column="1" Margin="0,4,0,4"/>

                <CheckBox x:Name="ActsAsSalesmanBox"
                          Content="Acts as Salesman"
                          Grid.Row="4" Grid.ColumnSpan="2"
                          Margin="0,12,0,0"/>
            </Grid>

            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                <Button Content="Save" Width="100" Margin="0,0,8,0" Click="Save_Click"/>
                <Button Content="Cancel" Width="100" Click="Cancel_Click"/>
            </StackPanel>
        </Grid>
    </DockPanel>
</Window>
