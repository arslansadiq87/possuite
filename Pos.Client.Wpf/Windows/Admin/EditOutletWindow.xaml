<Window x:Class="Pos.Client.Wpf.Windows.Admin.EditOutletWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Outlet" Width="520" Height="360"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="16">
        <!-- FIX: must be Grid.RowDefinitions -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0"
                   Text="Outlet Details"
                   Margin="0,0,0,8"
                   FontSize="18"
                   FontWeight="SemiBold"/>

        <!-- Form -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="160"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Code" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,8"/>
            <TextBox  Grid.Row="0" Grid.Column="1"
                      Text="{Binding Code, UpdateSourceTrigger=PropertyChanged}"
                      MaxLength="16"
                      Margin="0,0,0,8"/>

            <TextBlock Text="Name" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,8"/>
            <TextBox  Grid.Row="1" Grid.Column="1"
                      Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                      MaxLength="80"
                      Margin="0,0,0,8"/>

            <TextBlock Text="Address" Grid.Row="2" Grid.Column="0" VerticalAlignment="Top" Margin="0,4,8,0"/>
            <TextBox  Grid.Row="2" Grid.Column="1"
                      Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}"
                      AcceptsReturn="True"
                      TextWrapping="Wrap"
                      VerticalScrollBarVisibility="Auto"
                      MinHeight="80"
                      Margin="0,0,0,8"/>

            <TextBlock Text="Active" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <CheckBox  Grid.Row="3" Grid.Column="1" IsChecked="{Binding IsActive}"/>
        </Grid>

        <!-- Footer -->
        <DockPanel Grid.Row="2" LastChildFill="False" Margin="0,8,0,0">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                <Button Content="Cancel" Width="96" IsCancel="True" Margin="0,0,8,0"/>
                <Button Content="Save"   Width="96" IsDefault="True" Click="Save_Click"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
