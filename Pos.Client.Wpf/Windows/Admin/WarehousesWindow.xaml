<Window x:Class="Pos.Client.Wpf.Windows.Admin.WarehousesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Warehouses" Height="520" Width="760"
        WindowStartupLocation="CenterOwner"
        KeyDown="Window_KeyDown">
    <DockPanel>
        <ToolBar DockPanel.Dock="Top" Style="{StaticResource ModernToolBar.Compact}">
            <Button x:Name="AddBtn" Content="New" Padding="12,4" Click="Add_Click"/>
            <Button x:Name="EditBtn" Content="Edit" Padding="12,4" Margin="8,0,0,0" Click="Edit_Click"/>
            <Button x:Name="EnableBtn" Content="Enable" Padding="12,4" Margin="8,0,0,0" Click="Enable_Click"/>
            <Button x:Name="DisableBtn" Content="Disable" Padding="12,4" Margin="8,0,0,0" Click="Disable_Click"/>
            <Separator/>
            <CheckBox x:Name="ShowInactive" Content="Show inactive" Margin="8,0,0,0"
                      Checked="FilterChanged" Unchecked="FilterChanged"/>
            <Separator/>
            <TextBox x:Name="SearchBox" Width="240" Margin="8,0,0,0"
                     VerticalContentAlignment="Center"
                     TextChanged="SearchBox_TextChanged"
                     ToolTip="Search by name/city/phone"/>
        </ToolBar>

        <DataGrid x:Name="WarehousesGrid"
                  Margin="8"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="False"
                  SelectionMode="Single"
                  SelectionChanged="Grid_SelectionChanged"
                  MouseDoubleClick="Grid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Code" Binding="{Binding Code, UpdateSourceTrigger=PropertyChanged}" Width="60" IsReadOnly="True"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Width="*" />
                <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" Width="70" />
                <DataGridTextColumn Header="City" Binding="{Binding City, UpdateSourceTrigger=PropertyChanged}" Width="100" />
                <DataGridTextColumn Header="Phone" Binding="{Binding Phone, UpdateSourceTrigger=PropertyChanged}" Width="100" />
                <DataGridTextColumn Header="Note" Binding="{Binding Note, UpdateSourceTrigger=PropertyChanged}" Width="220" />
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</Window>
