<Window x:Class="Pos.Client.Wpf.Windows.Admin.EditCategoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:infra="clr-namespace:Pos.Client.Wpf.Infrastructure"
        Title="Category" Height="200" Width="400" WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="110"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Name:" VerticalAlignment="Center" />
            <TextBox x:Name="NameBox" Grid.Column="1" Margin="8,0,0,8" />

            <TextBlock Grid.Row="1" Text="Active:" VerticalAlignment="Center" />
            <CheckBox x:Name="IsActiveBox" Grid.Row="1" Grid.Column="1" Margin="8,0,0,0" IsChecked="True"/>
        </Grid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Save" Width="90" Margin="0,0,8,0" Click="Save_Click" Style="{StaticResource Btn.GlobalPrimary}"/>
            <Button Content="Cancel" Width="90" Margin="0,0,8,0"  Command="{x:Static infra:AppCommands.CloseWindow}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" Style="{StaticResource Btn.GlobalPrimary}"/>
        </StackPanel>
    </Grid>
</Window>
