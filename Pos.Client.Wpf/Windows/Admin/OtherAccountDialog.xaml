<Window x:Class="Pos.Client.Wpf.Windows.Admin.OtherAccountDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding DialogTitle}" Height="320" Width="520" WindowStartupLocation="CenterOwner">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Code:" VerticalAlignment="Center"/>
        <TextBox x:Name="CodeBox" Grid.Row="0" Margin="80,0,0,6" Width="160"/>

        <TextBlock Text="Name:" Grid.Row="1" VerticalAlignment="Center"/>
        <TextBox x:Name="NameBox" Grid.Row="1" Margin="80,0,0,6"/>

        <TextBlock Text="Phone:" Grid.Row="2" VerticalAlignment="Center"/>
        <TextBox x:Name="PhoneBox" Grid.Row="2" Margin="80,0,0,6" Width="180"/>

        <TextBlock Text="Email:" Grid.Row="3" VerticalAlignment="Center"/>
        <TextBox x:Name="EmailBox" Grid.Row="3" Margin="80,0,0,6" Width="220"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save" Width="90" Margin="0,12,8,0" Click="Save_Click"/>
            <Button Content="Cancel" Width="90" Margin="0,12,0,0" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
