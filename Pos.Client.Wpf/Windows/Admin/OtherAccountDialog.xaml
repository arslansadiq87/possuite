<Window x:Class="Pos.Client.Wpf.Windows.Admin.OtherAccountDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding DialogTitle}"
        Height="320" Width="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="110"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Code -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Code:" VerticalAlignment="Center" />
        <TextBox   x:Name="CodeBox"
                   Grid.Row="0" Grid.Column="1"
                   Margin="0,4,0,6" Width="160"/>

        <!-- Name -->
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Name:" VerticalAlignment="Center" />
        <TextBox   x:Name="NameBox"
                   Grid.Row="1" Grid.Column="1"
                   Margin="0,4,0,6"/>

        <!-- Phone -->
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Phone:" VerticalAlignment="Center" />
        <TextBox   x:Name="PhoneBox"
                   Grid.Row="2" Grid.Column="1"
                   Margin="0,4,0,6" Width="180"/>

        <!-- Email -->
        <TextBlock Grid.Row="3" Grid.Column="0" Text="Email:" VerticalAlignment="Center" />
        <TextBox   x:Name="EmailBox"
                   Grid.Row="3" Grid.Column="1"
                   Margin="0,4,0,6" Width="220"/>

        <!-- Spacer / notes area if needed -->
        <Border Grid.Row="4" Grid.ColumnSpan="2"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="5" Grid.ColumnSpan="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,12,0,0">
            <Button Content="Save"   Width="90" Margin="0,0,8,0" Click="Save_Click"   Style="{StaticResource Btn.GlobalPrimary}"/>
            <Button Content="Cancel" Width="90"                 Click="Cancel_Click" Style="{StaticResource Btn.GlobalPrimary}"/>
        </StackPanel>
    </Grid>
</Window>
