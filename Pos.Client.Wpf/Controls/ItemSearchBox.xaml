<UserControl x:Class="Pos.Client.Wpf.Controls.ItemSearchBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="280">
    <Grid>
        <!-- The textbox -->
        <TextBox x:Name="SearchBox"
             Padding="8,6"
             VerticalContentAlignment="Center"
             TextChanged="SearchBox_TextChanged"
             PreviewKeyDown="SearchBox_PreviewKeyDown"/>

        <!-- Watermark overlay (shows when Text is empty) -->
        <TextBlock Text="Scan SKU/Barcode or type name"
               Foreground="#888"
               Margin="12,0,0,0"
               VerticalAlignment="Center"
               IsHitTestVisible="False">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding Text, ElementName=SearchBox}" Value=""/>
                                <Condition Binding="{Binding IsKeyboardFocusWithin, ElementName=SearchBox}" Value="False"/>
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Visibility" Value="Visible"/>
                        </MultiDataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <!-- Suggestions popup -->
        <Popup x:Name="Popup"
           PlacementTarget="{Binding ElementName=SearchBox}"
           Placement="Bottom"
           StaysOpen="False"
           AllowsTransparency="True">
            <Border Background="White" BorderBrush="#DDD" BorderThickness="1" CornerRadius="4"
              Padding="4" Width="{Binding ElementName=SearchBox, Path=ActualWidth}">
                <ListBox x:Name="List"
             MouseDoubleClick="List_MouseDoubleClick"
         PreviewKeyDown="List_PreviewKeyDown"
         BorderThickness="0"
         ScrollViewer.VerticalScrollBarVisibility="Auto"
         ScrollViewer.CanContentScroll="True"
         VirtualizingStackPanel.IsVirtualizing="True"
         VirtualizingStackPanel.VirtualizationMode="Recycling"
         MaxHeight="360"
         IsTextSearchEnabled="True"
         TextSearch.TextPath="DisplayName">

                    <!-- Keep keyboard behaviour by NOT disabling focus -->
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="4"/>
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="SnapsToDevicePixels" Value="True"/>
                            <!-- IMPORTANT: no Focusable=False here -->

                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Root"
                                        Property="Background"
                                        Value="#F3F6FF"/>
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="Root"
                                        Property="Background"
                                        Value="#E3ECFF"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>

                    <!-- Visual template with stock badge -->
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Left: name + SKU / Barcode -->
                                <StackPanel Orientation="Vertical"
                            Grid.Column="0"
                            Margin="0,0,8,0">
                                    <TextBlock Text="{Binding DisplayName}"
                               FontWeight="SemiBold"
                               TextTrimming="CharacterEllipsis"/>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Sku}"
                                   Foreground="#777"
                                   FontSize="11"
                                   Margin="0,0,6,0"/>
                                        <TextBlock Text="{Binding Barcode}"
                                   Foreground="#AAA"
                                   FontSize="11"/>
                                    </StackPanel>
                                </StackPanel>

                                <!-- Right: stock badge -->
                                <!-- Change OnHand to your actual stock property if different -->
                                <Border Grid.Column="1"
                        VerticalAlignment="Center"
                        Padding="8,2"
                        CornerRadius="10"
                        Background="#EEF7FF"
                        BorderBrush="#B5D4FF"
                        BorderThickness="1">
                                    <TextBlock Text="{Binding OnHand, StringFormat='{}{0:#,0} in stock'}"
                               FontSize="11"
                               FontWeight="SemiBold"
                               Foreground="#1A5A9E"/>
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

            </Border>
        </Popup>
    </Grid>
</UserControl>
